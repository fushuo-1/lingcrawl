// Stub: notification service removed in self-hosted trim
export function sendNotificationWithCustomDays(
  teamId: string,
  notificationType: any,
  days: number,
  force: boolean,
  immediate: boolean,
): Promise<void> {
  return Promise.resolve();
}
